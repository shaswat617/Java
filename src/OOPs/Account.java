package OOPs;

public class Account {
    private double balance;

    public static void main(String[] args) {

    }
    public double getBalance(String userName, String password) { //method declaration : modifier + returntype + methodname + argument
        // username and password authentication logic required // within curly braces we define the method
        return balance;
    }
    // abstract class : a class which has methods with only declaration and without implementation
    // interface : something which has methods with only declaration and without implementation
}

